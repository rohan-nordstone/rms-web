export enum StationType {
  TV = "tv",
  RADIO = "radio",
  NEWSPAPER = "newspaper",
}

export enum StationVerificationStatus {
  PENDING = "pending",
  APPROVED = "approved",
  DENIED = "denied",
}

export enum StationApprovalStatus {
  PENDING = "pending",
  APPROVED = "approved",
  DENIED = "denied",
}
